[phases.setup]
nixPkgs = ["python311", "gcc", "pkgconfig", "poppler"]

[phases.install]
cmds = ["python -m venv /opt/venv && . /opt/venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt"]

[start]
cmd = "python app.py"
