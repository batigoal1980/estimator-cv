# PDF Processing (keeping same versions as local)
PyPDF2==3.0.1
pdf2image==1.16.3
Pillow==10.4.0

# Image Processing (no opencv for Railway, but same numpy/matplotlib versions)
numpy==1.26.0
matplotlib==3.8.0
seaborn==0.13.0
scipy==1.11.4

# Utilities (same versions)
tqdm==4.66.1
requests==2.31.0
pyyaml==6.0.1

# Web Interface (same versions)
Flask==3.0.3
Werkzeug==3.0.3

# CubiCasa API (same versions)
supervision>=0.16.0
inference-sdk
roboflow==1.2.6
